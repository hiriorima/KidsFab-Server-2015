# Use the README file as a reference to customize settings.

Login:
  fields:
    default: userid, password_hash, password_salt, kie
    form: userid, password_hash, password_salt, kie
  relationships: 
  application: Application
