<% provide(:title, "新規登録") %>
平文での通信だから個人情報に関わらないものを<br>
<% if logged_in? %>
<% else %>
<div class="form-wrapper">
  <h1>新規登録</h1>
  <%= form_tag(action: 'add_user') do %>
  <form>
    <div class="form-item">
      <%= label_tag :ユーザID入力 %>
      <%= text_field_tag :userid %>
    </div>
    <div class="form-item">
      <%= label_tag :パスワード入力 %>
      <%= password_field_tag :password %>
    </div>
    <div class="form-item">
      <%= label_tag :パスワード再入力 %>
      <%= password_field_tag :password_confirmation %>  
    </div>
    <div class="button-panel">
      <%= submit_tag "新規登録", class: "button" %>
    </div>
  </form>
  <% end %>
  <% end %>
  <div class="form-footer">
    <p><%= link_to("既にあるアカウントでログイン", action: 'login') %></p>
  </div>
</div>

